schema: registry@1.0

# Machine-readable node registry — git clone URLs + versions.
# Used by `ire sync` and the run-workflow skill for git clone.
# Commit pinning removed — agents clone the latest main branch.
# See manifest.yaml for agent-oriented semantic metadata.

nodes:
  geo-microarray-processing:
    source: git
    url: https://github.com/bobo-xxx/medflow-geo-microarray.git
    versions:
      - version: "1.0.0"

  batch-correction:
    source: git
    url: https://github.com/bobo-xxx/medflow-batch-correction.git
    versions:
      - version: "1.0.0"

  differential-analysis:
    source: git
    url: https://github.com/bobo-xxx/medflow-differential-analysis.git
    versions:
      - version: "1.0.0"

  univariate-filter:
    source: git
    url: https://github.com/bobo-xxx/medflow-univariate-filter.git
    versions:
      - version: "1.0.0"

  go-kegg-enrichment:
    source: git
    url: https://github.com/bobo-xxx/medflow-go-kegg-enrichment.git
    versions:
      - version: "1.0.0"
